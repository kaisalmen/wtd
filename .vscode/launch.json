
{
    "version": "2.0.0",
    "configurations": [
        {
            "type": "chrome",
            "request": "launch",
            "name": "Launch Chrome against localhost",
            "url": "http://localhost:8080/packages/examples/index.html",
            "webRoot": "${workspaceFolder}"
        }
    ]
}
